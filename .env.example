VITE_SUPABASE_URL=
VITE_SUPABASE_ANON_KEY=
# Cloudflare Turnstile site key (public) for the Apply form CAPTCHA.
# Create one at https://dash.cloudflare.com > Turnstile. If unset, a test key
# that always passes is used (no real protection).
VITE_TURNSTILE_SITE_KEY=